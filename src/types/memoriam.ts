export type MemoriamTribute = {
  id: string;
  deceasedId: string;
  authorId: string;
  content: string;
  photoUrls: string[];
  createdAt: string;
};

export type PrayerRecord = {
  id: string;
  deceasedId: string;
  authorId: string;
  createdAt: string;
};
